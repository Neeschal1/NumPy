import numpy as np

matrix = np.array([10, 20, 30, 40, 50])

# Indexing in python begins from 0.
# The last value can be accessed by -1 index. And the elements from last can be accessed by -1, -2, -3 respectively. 
print(matrix[0]) 
print(matrix[3])
print(matrix[-1])